// 3Sum

/**
 * @param {number[]} nums
 * @return {number[][]}
 */
var threeSum = function (nums) {
  // 오름차순 정렬
  // for문
  // 현재 인덱스 +1(first) 과 nums.length-1(last) sum 0인지 확인
  // sum > 0 last--
  // sum < 0 first++
};
